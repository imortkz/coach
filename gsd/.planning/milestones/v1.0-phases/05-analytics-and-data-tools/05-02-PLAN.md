---
phase: 05-analytics-and-data-tools
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - ../scripts/gymcoach_scripts/export_import/__init__.py
  - ../scripts/gymcoach_scripts/export_import/exporter.py
  - ../scripts/gymcoach_scripts/export_import/importer.py
  - ../scripts/gymcoach_scripts/export_import/commands.py
  - ../scripts/gymcoach_scripts/cli.py
  - ../scripts/tests/test_export_import.py
autonomous: true
requirements: [SCRP-04]

must_haves:
  truths:
    - "Running `gymcoach export --format json` writes a JSON file with all exercises, programs, workouts, and settings"
    - "Running `gymcoach export --format csv` writes a CSV file with workout log rows (date, exercise, muscle_group, set_number, weight_kg, reps, is_warmup, program)"
    - "Running `gymcoach import <file>` replaces all existing data with the imported JSON data"
    - "Import automatically creates a backup JSON file before replacing data"
    - "Import shows a confirmation prompt before proceeding"
    - "Import reports count of imported entities on completion"
  artifacts:
    - path: "../scripts/gymcoach_scripts/export_import/exporter.py"
      provides: "Pure functions for JSON and CSV export"
      exports: ["export_all_json", "export_workout_csv"]
    - path: "../scripts/gymcoach_scripts/export_import/importer.py"
      provides: "Pure functions for JSON import with backup"
      exports: ["import_data", "create_backup"]
    - path: "../scripts/gymcoach_scripts/export_import/commands.py"
      provides: "Click commands for export and import"
      exports: ["export_cmd", "import_cmd"]
    - path: "../scripts/tests/test_export_import.py"
      provides: "Unit and integration tests for export/import"
      min_lines: 80
  key_links:
    - from: "../scripts/gymcoach_scripts/export_import/commands.py"
      to: "../scripts/gymcoach_scripts/export_import/exporter.py"
      via: "imports export functions"
      pattern: "from gymcoach_scripts.export_import.exporter import"
    - from: "../scripts/gymcoach_scripts/export_import/commands.py"
      to: "../scripts/gymcoach_scripts/export_import/importer.py"
      via: "imports import functions"
      pattern: "from gymcoach_scripts.export_import.importer import"
    - from: "../scripts/gymcoach_scripts/cli.py"
      to: "../scripts/gymcoach_scripts/export_import/commands.py"
      via: "cli.add_command for export and import"
      pattern: "cli\\.add_command\\(export_cmd"
---

<objective>
Build CLI export and import commands for data backup and restore.

Purpose: Fulfills SCRP-04 -- users can export all workout data to JSON or CSV, and import a previously exported JSON file to restore data.
Output: `gymcoach export` and `gymcoach import` top-level commands with JSON/CSV export and JSON-only import with auto-backup and replace-all strategy.
</objective>

<execution_context>
@/Users/vjkim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vjkim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-analytics-and-data-tools/05-CONTEXT.md
@.planning/phases/05-analytics-and-data-tools/05-RESEARCH.md
@.planning/phases/05-analytics-and-data-tools/05-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From ../scripts/gymcoach_scripts/db/models.py:
```python
class Exercise(Base):  # exercises table
    id, name, muscle_group, equipment, is_custom

class Program(Base):  # programs table
    id, name, created_at, rest_timer_disabled
    exercises: list[ProgramExercise]  # relationship

class ProgramExercise(Base):  # program_exercises table
    id, program_id, exercise_id, order
    sets: list[ProgramSet]  # relationship

class ProgramSet(Base):  # program_sets table
    id, program_exercise_id, set_number, target_reps, target_weight_kg, is_warmup

class Workout(Base):  # workouts table
    id, program_id, started_at, completed_at
    sets: list[WorkoutSet]  # relationship

class WorkoutSet(Base):  # workout_sets table
    id, workout_id, exercise_id, set_number, weight_kg, reps, is_warmup
    exercise: Exercise  # relationship

class Setting(Base):  # settings table
    key (PK), value
```

From ../scripts/gymcoach_scripts/db/engine.py:
```python
SessionLocal  # sessionmaker factory
```

From ../scripts/gymcoach_scripts/cli.py (after Plan 01):
```python
cli.add_command(migrate)
cli.add_command(seed)
cli.add_command(analytics)  # added by Plan 01
```

Delete order for FK safety: WorkoutSet -> Workout -> ProgramSet -> ProgramExercise -> Program -> Exercise -> Setting
Insert order (reverse): Setting -> Exercise -> Program -> ProgramExercise -> ProgramSet -> Workout -> WorkoutSet
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export/import logic and tests</name>
  <files>
    ../scripts/gymcoach_scripts/export_import/__init__.py,
    ../scripts/gymcoach_scripts/export_import/exporter.py,
    ../scripts/gymcoach_scripts/export_import/importer.py,
    ../scripts/tests/test_export_import.py
  </files>
  <behavior>
    - export_all_json(session) returns dict with keys: version, exported_at, exercises, programs, workouts, settings
    - Exported programs include nested exercises with nested sets
    - Exported workouts include nested sets
    - export_workout_csv(session) returns list of dicts with keys: date, exercise, muscle_group, set_number, weight_kg, reps, is_warmup, program
    - CSV rows are ordered by workout date, then exercise, then set_number
    - Program name is resolved for each workout (None if no program)
    - import_data(session, data, backup_path) deletes all existing data and inserts from data dict
    - Import respects FK order: delete children first, insert parents first
    - import_data returns counts dict: {"exercises": N, "programs": N, "workouts": N, "settings": N}
    - Round-trip test: export then import preserves all data (exercise count, workout count, set values match)
    - create_backup(session, backup_path) writes current data as JSON to backup_path
    - Import with empty data dict clears all data (valid use case)
  </behavior>
  <action>
    Work in ../scripts/ repo.

    1. Create `gymcoach_scripts/export_import/__init__.py` (empty).

    2. Create `gymcoach_scripts/export_import/exporter.py`:
       - `export_all_json(session) -> dict` -- query all entities with eager loading for relationships. Build nested dict structure per CONTEXT.md spec. Include "version": "1.0" and "exported_at" ISO timestamp. Use selectinload for Program.exercises and Workout.sets to avoid N+1.
       - `export_workout_csv(session) -> list[dict]` -- query WorkoutSet joined with Exercise and Workout (and Program via Workout.program_id). Build flat rows: date (Workout.started_at ISO), exercise (Exercise.name), muscle_group (Exercise.muscle_group), set_number, weight_kg, reps, is_warmup, program (Program.name or empty string). Order by started_at, exercise name, set_number.
       - `write_csv_file(rows: list[dict], path: Path)` -- use csv.DictWriter to write rows to file. Column order: date, exercise, muscle_group, set_number, weight_kg, reps, is_warmup, program.

    3. Create `gymcoach_scripts/export_import/importer.py`:
       - `create_backup(session, backup_path: Path)` -- call export_all_json, write to backup_path as indented JSON.
       - `import_data(session, data: dict, backup_path: Path) -> dict[str, int]` -- create backup first, then delete all data in FK-safe order (WorkoutSet, Workout, ProgramSet, ProgramExercise, Program, Exercise, Setting), then insert in reverse order. For each entity type, create model instances from the data dict. Maintain ID mapping if IDs are included. Commit at the end. Return counts of each entity imported. Wrap in try/except to rollback on failure.

    4. Create `tests/test_export_import.py` with tests for all behaviors. Reuse the seed_workout_data fixture from conftest.py (added in Plan 01). Test: JSON export structure, CSV export rows, import replaces data, import creates backup file, round-trip preserves data, empty import clears data.

    RED-GREEN: Write tests first, confirm failures, implement, confirm passes.
  </action>
  <verify>
    <automated>cd /Users/vjkim/Code/TEST_GSD/scripts && uv run pytest tests/test_export_import.py -x -v</automated>
  </verify>
  <done>Export produces correct JSON structure (nested entities) and CSV (flat workout log rows). Import deletes existing data in FK order, inserts new data in reverse order, creates backup before replacing. Round-trip test confirms data integrity. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Export/import Click commands and CLI wiring</name>
  <files>
    ../scripts/gymcoach_scripts/export_import/commands.py,
    ../scripts/gymcoach_scripts/cli.py
  </files>
  <action>
    Work in ../scripts/ repo.

    1. Create `gymcoach_scripts/export_import/commands.py`:
       - `@click.command("export") def export_cmd(format, output)`:
         - Options: `--format` (type=click.Choice(["json", "csv"]), default="json"), `-o` / `--output` (default None, auto-generates filename: `gymcoach-export-{YYYYMMDD}.{format}` in current directory).
         - Create session, call export_all_json or export_workout_csv + write_csv_file based on format.
         - For JSON: write json.dumps(data, indent=2, default=str) to output path.
         - Print success message with path and entity counts using Rich console.
         - try/finally to close session.

       - `@click.command("import") def import_cmd(file)`:
         - Argument: FILE (type=click.Path(exists=True)).
         - Read and parse JSON file. Validate it has expected top-level keys (exercises, programs, workouts).
         - Show import summary with Rich console: count of each entity type.
         - Print "[bold red]WARNING: This will replace ALL existing data.[/bold red]"
         - Use `click.confirm("Continue?")` -- abort if declined.
         - Create session, call import_data with backup_path = `backup-{YYYYMMDD-HHMMSS}.json`.
         - Print success: "Import complete. Backup saved to {path}" with entity counts.
         - try/finally to close session.

    2. Update `gymcoach_scripts/cli.py`: import export_cmd and import_cmd from export_import.commands, add:
       ```python
       cli.add_command(export_cmd, "export")
       cli.add_command(import_cmd, "import")
       ```

    3. Verify CLI help works for both commands.
  </action>
  <verify>
    <automated>cd /Users/vjkim/Code/TEST_GSD/scripts && uv run python -m gymcoach_scripts export --help && uv run python -m gymcoach_scripts import --help && uv run pytest tests/test_export_import.py -x -v</automated>
  </verify>
  <done>Export command accessible as `gymcoach export` with --format json|csv and -o flags. Import command accessible as `gymcoach import FILE` with confirmation prompt. Both show correct --help output. All export/import tests still pass.</done>
</task>

</tasks>

<verification>
1. `cd ../scripts && uv run pytest tests/test_export_import.py -x -v` -- all export/import tests pass
2. `cd ../scripts && uv run python -m gymcoach_scripts export --help` -- shows --format and -o options
3. `cd ../scripts && uv run python -m gymcoach_scripts import --help` -- shows FILE argument
4. `cd ../scripts && uv run python -m gymcoach_scripts export --format json` -- creates JSON file (may have no data if DB empty)
5. `cd ../scripts && uv run python -m gymcoach_scripts export --format csv` -- creates CSV file
6. `cd ../scripts && uv run pytest tests/ -x -q` -- full test suite passes (including analytics tests from Plan 01)
</verification>

<success_criteria>
- `gymcoach export --format json` writes nested JSON with exercises, programs, workouts, settings
- `gymcoach export --format csv` writes flat CSV with workout log rows
- Default output filename is `gymcoach-export-{date}.{format}` in current directory
- `-o` flag overrides output path
- `gymcoach import FILE` shows summary, asks for confirmation, creates backup, replaces all data
- Import reports entity counts on completion
- Round-trip (export then import) preserves all data
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-analytics-and-data-tools/05-02-SUMMARY.md`
</output>
