---
phase: 01-foundation-and-schema
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - ../backend/pyproject.toml
  - ../backend/app/__init__.py
  - ../backend/app/main.py
  - ../backend/app/database.py
  - ../backend/app/config.py
  - ../backend/app/exercises/__init__.py
  - ../backend/app/exercises/models.py
  - ../backend/app/exercises/schemas.py
  - ../backend/app/exercises/routes.py
  - ../backend/app/programs/__init__.py
  - ../backend/app/programs/models.py
  - ../backend/app/programs/schemas.py
  - ../backend/app/workouts/__init__.py
  - ../backend/app/workouts/models.py
  - ../backend/app/workouts/schemas.py
  - ../backend/tests/__init__.py
  - ../backend/tests/conftest.py
  - ../backend/tests/test_health.py
  - ../backend/Dockerfile
  - ../frontend/Dockerfile
autonomous: false

requirements:
  - SCRP-01

must_haves:
  truths:
    - "FastAPI backend starts and serves GET /api/health returning {status: ok}"
    - "Backend connects to the same SQLite database at ../data/gymcoach.db"
    - "Health endpoint confirms database connectivity"
    - "Backend models match the canonical models in scripts repo"
    - "All three repos start their dev servers without errors"
  artifacts:
    - path: "../backend/app/main.py"
      provides: "FastAPI application with health endpoint and router includes"
      contains: "FastAPI"
    - path: "../backend/app/database.py"
      provides: "Engine, SessionLocal, get_db dependency"
      contains: "get_db"
    - path: "../backend/app/exercises/models.py"
      provides: "Exercise model (duplicated from scripts)"
      contains: "class Exercise"
    - path: "../backend/tests/test_health.py"
      provides: "Health endpoint test"
      contains: "test_health"
  key_links:
    - from: "../backend/app/main.py"
      to: "../backend/app/database.py"
      via: "get_db dependency injection"
      pattern: "get_db"
    - from: "../backend/app/database.py"
      to: "../data/gymcoach.db"
      via: "GYMCOACH_DB env var or default path"
      pattern: "GYMCOACH_DB"
---

<objective>
Scaffold the backend repo with FastAPI, duplicated SQLAlchemy models, health endpoint, and Dockerfiles for both backend and frontend.

Purpose: Complete the three-repo foundation so the full dev environment is operational. Backend proves the schema created by scripts is accessible via API.
Output: FastAPI serving /api/health with DB connectivity, Dockerfiles ready for future deployment.
</objective>

<execution_context>
@/Users/vjkim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vjkim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-schema/01-CONTEXT.md
@.planning/phases/01-foundation-and-schema/01-RESEARCH.md
@.planning/phases/01-foundation-and-schema/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold backend repo with FastAPI, models, and health endpoint</name>
  <files>
    ../backend/pyproject.toml
    ../backend/app/__init__.py
    ../backend/app/main.py
    ../backend/app/database.py
    ../backend/app/config.py
    ../backend/app/exercises/__init__.py
    ../backend/app/exercises/models.py
    ../backend/app/exercises/schemas.py
    ../backend/app/exercises/routes.py
    ../backend/app/programs/__init__.py
    ../backend/app/programs/models.py
    ../backend/app/programs/schemas.py
    ../backend/app/workouts/__init__.py
    ../backend/app/workouts/models.py
    ../backend/app/workouts/schemas.py
    ../backend/tests/__init__.py
    ../backend/tests/conftest.py
    ../backend/tests/test_health.py
  </files>
  <action>
    **Repo: ../backend/**

    1. Initialize the backend repo with uv:
       ```bash
       cd ../backend && uv init --app --bare
       uv add fastapi uvicorn sqlalchemy pydantic python-dotenv
       uv add --dev ruff pytest httpx
       ```
       Note: httpx is needed for FastAPI TestClient.

    2. Create `app/config.py`:
       - `DB_PATH` from `GYMCOACH_DB` env var, default to `os.path.join(os.path.dirname(__file__), '..', '..', 'data', 'gymcoach.db')`
       - Resolve to absolute path

    3. Create `app/database.py`:
       - Engine creation with same WAL mode + foreign keys event listener as scripts
       - `SessionLocal = sessionmaker(bind=engine)`
       - `get_db()` generator for FastAPI Depends
       - Use synchronous `def` (not async) per user decision

    4. Create `app/main.py`:
       - FastAPI app instance with title "GymCoach API"
       - Health endpoint: `GET /api/health` returning `{"status": "ok", "database": "connected"}` (verify DB by running a simple query like `SELECT 1`)
       - Include routers from exercises, programs, workouts (empty for now) with `/api` prefix

    5. Duplicate models from scripts into domain folders (per research recommendation for independent git repos):
       - `app/exercises/models.py`: Exercise, Base
       - `app/programs/models.py`: Program, ProgramExercise (import Base from exercises)
       - `app/workouts/models.py`: Workout, WorkoutSet, Setting (import Base from exercises)
       - Add comment at top of each: `# Models duplicated from scripts/gymcoach_scripts/db/models.py (canonical source)`
       - NOTE: These models are for ORM queries only. Backend does NOT run migrations. Schema is managed by scripts repo.

    6. Create stub Pydantic schemas:
       - `app/exercises/schemas.py`: ExerciseRead with `model_config = {"from_attributes": True}`
       - `app/programs/schemas.py`: ProgramRead, ProgramExerciseRead
       - `app/workouts/schemas.py`: WorkoutRead, WorkoutSetRead

    7. Create stub route files:
       - `app/exercises/routes.py`: empty APIRouter with prefix="/exercises", tags=["exercises"]
       - `app/programs/routes.py` is NOT needed yet (no routes), just the schemas
       - `app/workouts/routes.py` is NOT needed yet

    8. Create `tests/conftest.py`:
       - Fixture: in-memory SQLite engine with all tables created
       - Fixture: override `get_db` dependency for test isolation
       - Fixture: FastAPI TestClient using httpx

    9. Create `tests/test_health.py`:
       - Test GET /api/health returns 200 with `{"status": "ok"}`
       - Test that response includes database connectivity confirmation

    **IMPORTANT per CLAUDE.md:** All file writes use absolute paths targeting /Users/vjkim/Code/TEST_GSD/backend/. Git commit in backend repo with tag [P1-03].
  </action>
  <verify>
    <automated>cd /Users/vjkim/Code/TEST_GSD/backend && uv run pytest tests/test_health.py -x -v</automated>
  </verify>
  <done>
    - FastAPI app created with health endpoint at /api/health
    - Models duplicated from scripts with source comment
    - Pydantic schemas defined with from_attributes
    - Health test passes
    - `uv run uvicorn app.main:app` starts without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Dockerfiles for backend and frontend</name>
  <files>
    ../backend/Dockerfile
    ../backend/.dockerignore
    ../frontend/Dockerfile
    ../frontend/.dockerignore
  </files>
  <action>
    **Repos: ../backend/ and ../frontend/**

    1. Create `../backend/Dockerfile`:
       - Multi-stage build
       - Base: python:3.12-slim
       - Install uv, copy pyproject.toml and uv.lock, install dependencies
       - Copy app/ directory
       - CMD: `["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]`
       - Create `.dockerignore` (tests/, .git/, __pycache__/, .venv/)

    2. Create `../frontend/Dockerfile`:
       - Multi-stage build
       - Build stage: node:20-slim, npm install, npm run build
       - Serve stage: nginx:alpine, copy dist/ to /usr/share/nginx/html
       - Include nginx config for SPA routing (try_files $uri /index.html)
       - Create `.dockerignore` (node_modules/, .git/, dist/)

    **IMPORTANT per CLAUDE.md:** Git commit in each repo with tag [P1-03].
  </action>
  <verify>
    <automated>test -f /Users/vjkim/Code/TEST_GSD/backend/Dockerfile && test -f /Users/vjkim/Code/TEST_GSD/frontend/Dockerfile && echo "Dockerfiles exist"</automated>
  </verify>
  <done>
    - Backend Dockerfile exists with multi-stage Python build
    - Frontend Dockerfile exists with multi-stage Node + nginx build
    - Both have .dockerignore files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all three repos start and work together</name>
  <files>none</files>
  <action>
    Human verification of the complete Phase 1 foundation. Claude runs automated smoke tests first, then user verifies visual/interactive elements.

    **What was built:**
    - Scripts repo: SQLAlchemy models, Alembic migrations, exercise seed script
    - Backend repo: FastAPI with health endpoint, database connectivity
    - Frontend repo: Vue 3 + Vite + Tailwind with router and stub views

    **Steps for user to verify:**

    1. Run migration and seed:
       ```bash
       cd /Users/vjkim/Code/TEST_GSD/scripts && uv run python -m gymcoach_scripts migrate && uv run python -m gymcoach_scripts seed
       ```
       Expected: tables created, 50 exercises seeded

    2. Start backend (in a terminal):
       ```bash
       cd /Users/vjkim/Code/TEST_GSD/backend && uv run uvicorn app.main:app --reload
       ```
       Visit http://localhost:8000/api/health - expect `{"status": "ok", "database": "connected"}`
       Visit http://localhost:8000/docs - expect Swagger UI

    3. Start frontend (in another terminal):
       ```bash
       cd /Users/vjkim/Code/TEST_GSD/frontend && npm run dev
       ```
       Visit http://localhost:5173 - expect Vue app with navigation bar
       Click through Exercises, Programs, Workout, History links - each shows stub view
       Tailwind styles should be applied (visible padding, heading styles)

    4. Verify database:
       ```bash
       sqlite3 /Users/vjkim/Code/TEST_GSD/data/gymcoach.db ".tables"
       sqlite3 /Users/vjkim/Code/TEST_GSD/data/gymcoach.db "SELECT COUNT(*) FROM exercises;"
       sqlite3 /Users/vjkim/Code/TEST_GSD/data/gymcoach.db "PRAGMA journal_mode;"
       ```
       Expected: 6 tables, 50 exercises, WAL mode

    **Resume signal:** Type "approved" or describe issues.
  </action>
  <verify>
    <automated>cd /Users/vjkim/Code/TEST_GSD/backend && uv run pytest -x -q && cd /Users/vjkim/Code/TEST_GSD/scripts && uv run pytest -x -q && cd /Users/vjkim/Code/TEST_GSD/frontend && npm run build</automated>
  </verify>
  <done>
    - All automated tests pass across backend and scripts repos
    - Frontend builds successfully
    - User confirms: backend health endpoint responds, frontend renders with navigation, database has 50 exercises in WAL mode
  </done>
</task>

</tasks>

<verification>
Backend tests:
```bash
cd /Users/vjkim/Code/TEST_GSD/backend && uv run pytest -x -v
```

Scripts tests:
```bash
cd /Users/vjkim/Code/TEST_GSD/scripts && uv run pytest -x -v
```

Frontend build:
```bash
cd /Users/vjkim/Code/TEST_GSD/frontend && npm run build
```
</verification>

<success_criteria>
- Backend starts and serves /api/health with DB connectivity
- Backend models match scripts models structure
- Dockerfiles exist for backend and frontend
- All three repos verified working together (migration -> seed -> backend API -> frontend renders)
- Health endpoint test passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-schema/01-03-SUMMARY.md`
</output>
