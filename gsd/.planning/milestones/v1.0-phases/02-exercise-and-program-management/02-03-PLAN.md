---
phase: 02-exercise-and-program-management
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - ../frontend/src/App.vue
  - ../frontend/src/views/ExercisesView.vue
  - ../frontend/src/stores/exercises.ts
  - ../frontend/src/main.css
autonomous: true
requirements: [EXER-01, EXER-02]

must_haves:
  truths:
    - "Navigation shows top navbar on desktop (>=768px) and bottom tab bar on mobile (<768px)"
    - "Exercises are grouped by muscle group in collapsible sections"
    - "Equipment dropdown filter narrows the exercise list"
    - "Text search filters exercises by name (case-insensitive)"
    - "Custom exercises show a 'Custom' badge"
    - "Clicking [+] on a muscle group header shows an inline form for creating a custom exercise"
    - "Custom exercises can be edited and deleted; seeded exercises are read-only"
  artifacts:
    - path: "../frontend/src/App.vue"
      provides: "Responsive navigation shell (desktop navbar + mobile tab bar)"
      min_lines: 40
    - path: "../frontend/src/views/ExercisesView.vue"
      provides: "Exercise library with grouped list, filters, inline CRUD"
      min_lines: 100
    - path: "../frontend/src/stores/exercises.ts"
      provides: "Pinia store connected to exercise API with filter params"
      min_lines: 30
  key_links:
    - from: "../frontend/src/stores/exercises.ts"
      to: "/api/exercises"
      via: "fetch with query params"
      pattern: "fetch.*api/exercises"
    - from: "../frontend/src/views/ExercisesView.vue"
      to: "../frontend/src/stores/exercises.ts"
      via: "useExercisesStore()"
      pattern: "useExercisesStore"
---

<objective>
Responsive navigation shell (desktop navbar / mobile tab bar) and the full Exercise Library UI with grouped list, filters, and inline custom exercise CRUD.

Purpose: Delivers the exercise browsing and management experience -- the first real user-facing feature.
Output: Working /exercises page with grouped, filterable exercise list and inline create/edit/delete for custom exercises.
</objective>

<execution_context>
@/Users/vjkim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vjkim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-exercise-and-program-management/02-CONTEXT.md
@.planning/phases/02-exercise-and-program-management/02-RESEARCH.md
@.planning/phases/02-exercise-and-program-management/02-01-SUMMARY.md

<interfaces>
<!-- Exercise API from Plan 01 -->

From ../backend/app/exercises/routes.py (after Plan 01):
```
GET  /api/exercises?muscle_group=X&equipment=X&search=X -> ExerciseRead[]
POST /api/exercises {name, muscle_group, equipment} -> ExerciseRead (201)
PUT  /api/exercises/:id {name?, muscle_group?, equipment?} -> ExerciseRead
DELETE /api/exercises/:id -> 204 (or 403 if seeded, 409 if in-use)
```

From ../frontend/src/types/index.ts:
```typescript
export interface Exercise {
  id: number; name: string; muscle_group: string; equipment: string; is_custom: boolean
}
```

From ../frontend/src/stores/exercises.ts (current stub):
```typescript
export const useExercisesStore = defineStore('exercises', () => {
  const exercises = ref<Exercise[]>([])
  const loading = ref(false)
  async function fetchExercises() { /* TODO */ }
  return { exercises, loading, fetchExercises }
})
```

From ../frontend/src/App.vue (current):
```vue
<!-- Desktop-only top navbar with 4 RouterLink items -->
<!-- Needs: mobile bottom tab bar, responsive switch at md: breakpoint -->
```

From ../frontend/src/router/index.ts:
```typescript
// Routes: / -> /exercises, /exercises, /programs, /workout, /history
// All lazy-loaded
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive navigation shell (desktop navbar + mobile tab bar)</name>
  <files>../frontend/src/App.vue, ../frontend/src/main.css</files>
  <action>
  Refactor `../frontend/src/App.vue` to implement adaptive navigation:

  1. **Desktop navbar (hidden on mobile):** Keep the existing top navbar structure but wrap it with `class="hidden md:flex ..."`. Keep the "GymCoach" brand text and 4 navigation links (Exercises, Programs, Workout, History). Clean minimal style per user decision: white background, subtle border-b, muted text colors, blue active state.

  2. **Mobile bottom tab bar (hidden on desktop):** Add a `<nav>` element fixed to the bottom of the viewport with `class="fixed bottom-0 left-0 right-0 md:hidden ..."`. Show the same 4 navigation items as icon+label stacked vertically (use simple SVG icons or emoji placeholder -- keep it minimal). White background, subtle border-t, active item in blue.

  3. **Main content area:** Add `class="pb-16 md:pb-0"` to the `<main>` element so content doesn't hide behind the mobile tab bar.

  4. **Visual style:** Apply the Linear/Notion-inspired clean minimal aesthetic:
     - Background: gray-50 (already set)
     - Navbar: white bg, gray-200 border, gray-600 text, blue-600 active
     - Generous whitespace, `max-w-5xl` container (already set)
     - Font: system default is fine (Inter-like from Tailwind defaults)

  5. **Optionally add to main.css:** Any Tailwind v4 custom CSS variables for the color palette if needed. Remember: Tailwind v4 uses `@import "tailwindcss"`, no config file.

  **Git commit in frontend:** `feat: responsive navigation with desktop navbar and mobile tab bar [P2-03]`
  </action>
  <verify>
    <automated>cd ../frontend && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>App shows top navbar on desktop (>=768px viewport) and bottom tab bar on mobile (<768px). Both nav elements have 4 items linking to correct routes. Active route is visually distinct. Clean minimal aesthetic applied.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise library UI with grouped list, filters, and inline CRUD</name>
  <files>../frontend/src/views/ExercisesView.vue, ../frontend/src/stores/exercises.ts</files>
  <action>
  1. **Update Pinia store** `../frontend/src/stores/exercises.ts`:
     - `fetchExercises(params?: { muscle_group?: string; equipment?: string; search?: string })`: Build URLSearchParams from provided filters, fetch from `/api/exercises?...`, store result in `exercises` ref.
     - Add `createExercise(data: { name: string; muscle_group: string; equipment: string }): Promise<Exercise>`: POST to `/api/exercises`, return created exercise, then re-fetch list.
     - Add `updateExercise(id: number, data: { name?: string; muscle_group?: string; equipment?: string }): Promise<Exercise>`: PUT to `/api/exercises/:id`, return updated exercise, then re-fetch.
     - Add `deleteExercise(id: number): Promise<void>`: DELETE to `/api/exercises/:id`, then re-fetch. Handle 409 (in-use) by throwing with error message.
     - Add `error` ref for error states.

  2. **Build ExercisesView.vue** `../frontend/src/views/ExercisesView.vue`:

     **Header area:**
     - Page title "Exercises"
     - Search input (text, placeholder "Search exercises...", debounced 300ms)
     - Equipment dropdown filter (populated from distinct equipment values in loaded exercises, plus "All" option)

     **Grouped list:**
     - Computed property groups exercises by `muscle_group`, sorted alphabetically
     - Each muscle group section has:
       - Collapsible header: muscle group name + exercise count + expand/collapse chevron + [+] button
       - Default: collapse sections with >8 exercises, expand smaller ones (Claude's discretion)
       - Exercise list within section: name, equipment tag, "Custom" badge if is_custom=true
       - For custom exercises: show edit (pencil icon) and delete (trash icon) buttons on hover/always on mobile

     **Inline create form:**
     - Clicking [+] next to a muscle group header reveals an inline form WITHIN that section (not a modal)
     - Form fields: name input, equipment dropdown (Barbell, Dumbbell, Cable, Machine, Bodyweight, Other)
     - Muscle group is pre-filled from the section header (not editable in this form)
     - Submit and Cancel buttons. On submit: call store.createExercise(), close form on success
     - Validation: name required, show inline error if empty

     **Inline edit:**
     - Clicking edit on a custom exercise replaces the exercise row with an inline edit form
     - Same fields as create: name, equipment dropdown, muscle_group dropdown (can change group)
     - Save and Cancel buttons

     **Delete:**
     - Clicking delete shows a simple confirm (window.confirm is fine for v1)
     - On 409 (in-use), show an alert with the error message from API

     **Loading and empty states:**
     - Show a subtle loading spinner/skeleton while fetching
     - Empty state: "No exercises found" with suggestion to adjust filters

     **Style:** Clean minimal -- lots of whitespace, subtle gray borders between items, muted text for secondary info (equipment), small badges. Use Tailwind utility classes. No custom CSS needed beyond what Tailwind provides.

  **Git commit in frontend:** `feat: exercise library with grouped list, filters, and inline CRUD [P2-03]`
  </action>
  <verify>
    <automated>cd ../frontend && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ExercisesView shows exercises grouped by muscle group in collapsible sections. Equipment filter and text search work. Custom exercises show badge and have edit/delete actions. Inline create form works per muscle group section. All connected to live API.</done>
</task>

</tasks>

<verification>
- `cd ../frontend && npx vue-tsc --noEmit` passes (no type errors)
- `cd ../frontend && npm run build` succeeds (production build)
- Navigation is responsive (navbar desktop, tab bar mobile)
- Exercise list loads from API, grouped by muscle group
- Filters narrow the list
- Custom exercise CRUD works inline
</verification>

<success_criteria>
- Responsive navigation adapts at 768px breakpoint
- Exercise library displays grouped, filterable list from API
- Custom exercise create/edit/delete works inline
- Seeded exercises are read-only (no edit/delete buttons)
- Clean minimal aesthetic consistent with Linear/Notion style
</success_criteria>

<output>
After completion, create `.planning/phases/02-exercise-and-program-management/02-03-SUMMARY.md`
</output>
